version: "1"
services:
  node:
    build: .
    command: "npm run start"
    working_dir: /home/node/personalsysbackend
    environment:
      - NODE_ENV=prod
    expose:
      - "3005"
    ports:
      - "3005:3005"